import {scaleHeight} from '../utils/DimensionEditor';

/**
 * Status bar height is 47 for 360x800 screen, and calculated accordingly for other screens.
 */
export const STATUS_BAR_HEIGHT = scaleHeight(47);
